<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To Do List</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
  <style>
    *{
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins',serif;
      /* outline: 1px solid red; */
    }
    .container{
      max-width: 75vw;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 5rem;
    }
    .heading{
      margin-top: 1.5rem;
      display: flex;
      justify-content: space-between;
    }
    .logo{
      font-size: 1.5rem;
      color: black;
    }
    span{
      font-size: 1rem;
      color: blue;
    }
    .nav{
      display: flex;
      gap: 1rem;
      font-size: 1rem;
      align-items: center;
    }
    .animate, .logout{
      text-decoration: none;
      color: blue;
      transition: all 0.3s ease-in-out;
    }
    .animate:hover{
      color: black;
      transform: scale(1.05);
    }
    .content-container{
      max-width: 400px;
      padding: 0.3rem 0;
      /* border: 1px solid; */
    }
    ul{
      padding-right: 1rem;
      display: flex;
      flex-direction: column;
      gap: 0.7rem;
      padding: 1rem 0;
      list-style-type: none;
    }
    li{
      color: blue;
      padding: 3px 10px;
      /* padding-bottom: 3px;
      padding-left: 10px; */
      border: 1px solid black;
      /* font-family: 'Playfair Display',sans-serif; */
      border-radius: 5px;
    }
    .add-btn{
      display: inline-block;
      text-decoration: none;
      width: 150px;
      border: 1px solid;
      background-color: white;
      border-radius: 5px;
      padding: 2px 0;
      text-align: center;
      cursor: pointer;
      transition: all 0.3s ease-in-out;
    }
    .add-btn:hover{
      background-color: blue;
      color: white;
      transform: scale(1.05);
    }
    .input-form{
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      max-width: 400px;
    }
    .data{
      border: 1px solid black;
      border-radius: 3px;
      padding: 2px 5px;
      font-size: 1rem;
    }
    .delete-btn{
      border: 2px solid red;
      border-radius: 50%;
      padding: 1px 0.45rem;
      text-decoration: none;
      cursor: pointer;
      background-color: white;
      color: red;
      font-size: 1rem;
      font-weight: 600;
      transition: all 0.3s ease-in-out;
    }
    .delete-btn:hover{
      background-color: red;
      color: white;
    }
    li{
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .logout{
      display: inline-block;
      border: 0.125rem solid red;
      background-color: red;
      color: white;
      padding: 0.125rem 0.5rem;
      border-radius: 2rem;
      transition: all 0.2s ease-in-out;
    }
    .logout:hover{
      background-color: white;
      color: red;
    }
    @media (max-width:550px) {
      .nav a{
        font-size: 0.8rem;
      }
    }
    @media (max-width:500px) {
      *{
        font-size: 14.5px;
      }
      .nav{
        gap: 0.8rem;
      }
      .container{
        max-width: 85vw;
      }
    }
    @media (max-width:370px) {
      *{
        font-size: 13.5px;
      }
      .container{
        max-width: 90vw;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="heading">
      <div class="logo">To <span>Do</span> List</div>
      <div class="nav">
        <a href="#" class="animate">Home</a>
        <a href="/login" class="animate">Login</a>
        <a href="/register" class="animate">Register</a>
        <a href="/logout" class="logout">Logout</a>
      </div>
    </div>
    <div class="content-container">
      <% if(data && data.length>0){ %>
        <ul>
          <%  data.forEach(item=>{ %>
                <li class="content"><%=item.description%><a href="/delete/<%=username%>/<%=item.id%>" class="delete-btn">D</a></li>
           <% }) %>
        </ul>
       <% } %>
    </div>
    <div class="list-create">
      <form action="/create/<%=username%>" method="post" class="input-form">
        <input type="text" name="inputData" placeholder="Enter the data..." class="data">
        <input type="submit" value="Add New List" class="add-btn">
      </form>
      <!-- <a href="/createlist" class="add-btn">Add New List</a> -->
    </div>
  </div>
  <script>
    let button = document.querySelector('delete-btn');
  
  </script>
</body>
</html>